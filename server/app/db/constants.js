const QUESTION = `question`
const CATEGORY = `category`
const CATEOGRY_HIERARCHY = `category_hierarchy`
const QUESTION_CATEGORISATION = `question_categorisation`

module.exports = {
  QUESTION,
  CATEGORY,
  CATEOGRY_HIERARCHY,
  QUESTION_CATEGORISATION
}